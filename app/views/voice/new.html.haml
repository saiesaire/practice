登録画面

= form_with model: @voice, local: true do |f|
  = f.label "名前"
  = f.text_area :name, placeholder: "名前"
  = f.text_area :anime, placeholder: "出演アニメ"
  = f.text_area :game, placeholder: "出演ゲーム"
  = f.text_area :other, placeholder: "その他"
  = f.text_area :title, placeholder: "その他出演アニメ"
  = f.submit "登録", class: "btn"

= link_to '一覧画面に戻る', voice_index_path, class: "btn"